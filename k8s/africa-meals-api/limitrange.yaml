apiVersion: v1
kind: ServiceAccount
metadata:
  name: africa-meals-api
  namespace: wise-eat
  labels:
    app.kubernetes.io/name: africa-meals-api
---
apiVersion: v1
kind: LimitRange
metadata:
  name: africa-meals-api-limits
  namespace: wise-eat
spec:
  limits:
    - type: Container
      default:
        memory: 512Mi
        cpu: 500m
      defaultRequest:
        memory: 512Mi
        cpu: 100m
      max:
        # Redis cache k8s : maxmemory 768mb + overhead → limite pod 1Gi (API reste ≤512Mi).
        memory: 2Gi
        cpu: "2"
      min:
        memory: 256Mi
        cpu: 50m
